<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Splash theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- No Title Bar-->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- white status bar -->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <!-- background -->
        <item name="android:windowBackground">@drawable/testsp</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

    <style name="FullTranslucent" parent="@style/Theme.AppCompat">
        <!--Titlebar 없애는 옵션
            true : Titlebar x
            false : Titlebar o
        -->
        <item name="windowNoTitle">false</item>
        <!-- 투명정도를 나타내는 색-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <!--
        android:backgroundDimEnabled
        true - 반투명 검정 /  false - 완전 투명
        -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="Theme.SplashEx" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <!-- Splash 화면의 백그라운드 컬러 지정 -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="S">@color/white</item>
        <!-- Splash 화면의 중앙에 위치할 아이콘 Drawable 지정 -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="S">@drawable/testsp</item>
        <!-- Splash 화면 중앙의 아이콘 주변 Background Color 지정 -->
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="S">@color/white</item>
        <!-- Splash 화면이 나타나는 시간 지정 -->
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="S">1000</item>
        <!-- Splash 화면 하단에 나타낼 브랜딩이미지 지정 (비권장) -->
<!--        <item name="android:windowSplashScreenBrandingImage">null</item>-->
    </style>

</resources>